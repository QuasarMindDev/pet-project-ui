import { extendTheme } from "native-base";

const defaultTheme = extendTheme({
    colors: {
        primary: {
            50: '#7000FF'
        },
        secondary: {
            50: '#F1F1F1'
        },
    }
})

export default defaultTheme;
